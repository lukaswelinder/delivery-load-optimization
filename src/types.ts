import { Delivery } from './Delivery';

/**
 * Coordinates used for delivery vertices.
 */
export type Coordinate = [x: number, y: number];

/**
 * Array of delivery IDs.
 */
export type Route = number[];
